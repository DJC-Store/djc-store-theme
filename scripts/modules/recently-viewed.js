define(['modules/jquery-mozu', 'underscore', "modules/api", "modules/backbone-mozu", "modules/models-product"],
    function ($, _, api, Backbone, ProductModels) {

        // this sets a cookie with an arbitrary value
		$(document).ready(function(){
			//allow json: $.cookie.json = true;
			//set cookie: $.cookie('dj-recent-products',{ one: 'ima cookie', two: 'also a cookie'}, { expires: 365, path: '/' });
			//read cookie: $.cookie('dj-recent-products');
		});
    });