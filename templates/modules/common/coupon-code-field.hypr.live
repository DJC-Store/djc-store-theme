        <div class="mz-l-formfieldgroup-cell">
            <label for="coupon-code" id="coupon-code-label">{{ labels.couponCode }}: </label>
        </div>
        <div class="mz-l-formfieldgroup-cell">
            <input type="text" id="coupon-code" name="coupon-code" value="{{ model.couponCode }}" data-mz-value="couponCode">
            <button type="button" id="cart-coupon-code" class="mz-button blue-light" data-mz-action="addCoupon" {% if not model.couponCode %} disabled="disabled" {% endif %}>{{ labels.apply }}</button>
        </div>